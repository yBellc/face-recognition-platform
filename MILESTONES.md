# 重点对象识别平台里程碑与成熟度验收

> 当前状态：P0 工程闭环完成。P0 完成不等于生产准确率达标，也不等于可以直接用于身份认定。

## P0 · 工程闭环（已完成）

目标：证明系统从对象入库到识别结果可以完整跑通。

- InsightFace `buffalo-l-v1` 人脸检测、特征提取和相似度候选匹配。
- 重点对象新增、删除、参考照片增删和文件夹批量导入。
- 单张现场图片上传，多人脸检测和彩色框定位。
- 每张人脸独立展示候选、相似度、质量说明和匹配过程。
- 人工确认、排除、存疑、无法判断、归档。
- 单图、项目级和全部项目 CSV 导出。
- 账号、角色、项目访问隔离、授权记录和基础审计日志。
- React 工作台、FastAPI 服务和持久化数据模型。

P0 退出条件：登录、建档、上传、识别、复核、导出、归档的主链路可重复运行；异常输入有明确错误提示；前后端构建和健康检查通过。

## P1 · 数据与算法验证（下一阶段，立项后立即做）

目标：回答“在本项目真实场景中，系统到底有多准”。

### 数据集

- 内部数据必须取得授权，并建立唯一对象编号、采集时间、来源和用途字段。
- 将数据拆成对象库、验证集、测试集三部分，严格防止同一张照片或同一连续帧泄漏到多个集合。
- 每名对象尽量覆盖正脸、左右侧脸、远近距离、不同光线、眼镜、遮挡和表情变化。
- 单独建立非重点对象、外观相似对象和无人脸/低质量图片集合。
- 对模糊、逆光、遮挡、车窗反光、小脸和多人重叠建立质量标签。

### 评测协议

- 1:1 验证：判断两张脸是否为同一人。
- 1:N 检索：现场人脸在重点对象名单中检索，并统计 Top-1、Top-5 和未命中。
- 以对象、日期和场景分组切分，不能随机把相邻视频帧混到训练和测试两侧。
- 固定模型版本、阈值版本、数据版本和评测脚本，所有结果可复现。

### 指标与阈值

- 误报率 FMR、漏报率 FNMR、真正率 TMR、拒识率和人工复核率。
- 以业务可接受的误报上限反推阈值，而不是直接沿用演示值 0.45/0.60/0.75。
- 输出阈值曲线、ROC/DET、混淆样本和置信区间。
- 低质量人脸进入“无法判断”，不能仅凭低分候选强行归因。
- 分别报告不同光线、角度、遮挡、年龄/性别等分组的性能差异。

P1 退出条件：有一份经授权的内部测试报告，明确 1:1 和 1:N 指标、阈值、样本量、失败案例和适用边界；评测结果由业务负责人签字确认。

## P2 · 生产安全与可靠性（P1 达标后）

目标：让系统在真实环境中安全、稳定、可恢复。

本轮已先落地的 P2 基线：CORS 改为环境白名单；增加安全响应头和敏感 API `no-store`；生产环境缺少 `FACE_AUTH_SECRET` 时拒绝启动；生产环境缺少 PostgreSQL 驱动时拒绝静默退化到 SQLite；上传文件类型/大小/真实解码校验；登录失败窗口限流；账号启停/角色/项目授权更新且每次受保护请求重新校验账号状态；Redis/Celery 队列探针、超时和丢 worker 重投；探针重试幂等清理；已归档数据保留期限预览与清理接口；授权撤回和只读审计查询；前端增加大屏模式入口。剩余 P2 工作仍需在部署环境逐项验收。

### 安全与隐私

- 将演示账号、默认密码、默认 HMAC 密钥全部移出代码，改用密钥管理和部署密文。
- CORS 从通配符改为正式域名白名单；启用 HTTPS、Cookie/Token 安全策略、登录失败限流和会话撤销。
- 生产数据库统一使用 PostgreSQL，SQLite 仅保留本地演示用途。
- 图片、特征向量、导出文件和备份分别加密，严格区分原图访问和特征访问权限。
- 完成个人信息保护影响评估、授权凭证、处理目的、最短保留期限、撤回授权和删除证明。
- 接入到期自动清理任务、备份保留策略和灾难恢复演练。当前已提供 `POST /api/v1/maintenance/retention`，默认只预览，正式定时任务接入前必须先做部署审批。
- 审计日志追加写入、不可被普通管理员修改，并记录导出、查看、删除和授权变更。

### 可靠性与性能

- Redis/Celery 队列正式化，支持重试、幂等、取消、超时和死信任务。
- 上传文件类型、大小、损坏、重复和恶意内容校验。
- 大对象库使用向量索引，压测 1 千、1 万、10 万对象规模的检索延迟。
- 记录成功率、失败率、p50/p95/p99 耗时、队列积压、GPU/CPU 和存储容量。当前已提供 `/api/v1/monitoring/summary` 与 `/api/v1/monitoring/queue`，GPU/存储容量告警仍需部署侧接入。
- 模型、阈值、数据集和代码版本绑定，支持一键回滚。

P2 退出条件：完成安全测试、容量压测、备份恢复演练和故障演练；核心接口有自动化回归测试；关键指标有告警和责任人。

## P3 · 小范围试运行与持续改进

目标：在可控范围内验证“领导给一批人员、每天上传图片”这一真实工作方式。

- 先以影子模式运行，不直接影响现有人工流程。
- 选定少量项目和人员，连续运行 2～4 周。
- 每周抽样复核误报、漏报、无法判断和低质量样本。
- 通过人工复核结果建立困难样本集，不直接把未经审核结果回灌模型。
- 按月发布模型/阈值评测报告，出现性能下降时回滚或重新校准。
- 完成操作手册、应急预案、权限审批流程和培训材料。

P3 退出条件：试运行期间无高风险权限或隐私事故；关键业务指标连续达标；业务负责人确认可以扩大范围。

## 当前公开数据结果的正确解读

- DrivFace 只有 4 个身份，适合验证车内域流程，不适合证明大规模识别能力。
- 当前 LFW 脚本只评分了 98 对样本，适合作为通用照片基线，不等同于现场图片或车内监控效果。
- 因此，公开数据结果只能作为“算法和流程可运行”的证据，不能写成“生产准确率”。

## 立项判断

当前可以申请进入 P1：内部授权数据接入和算法验证。

当前不建议直接承诺：正式部署准确率、自动身份认定、无人值守告警或跨场景稳定识别。

成熟系统的最终验收标准是：**数据可追溯、指标可复现、阈值有依据、低质量可拒识、结果有人复核、权限可隔离、数据可删除、故障可恢复、模型可回滚。**
