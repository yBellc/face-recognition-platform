# 数据目录说明

本目录不随代码仓库提交。这里可能包含公开数据、已授权内部数据或人脸图片，必须按数据授权范围存放在受控的私有目录或对象存储中。

建议结构：

```text
data/
  watchlist_benchmark/
    enrollment/<对象编号>/   # 参考照片
    probe/<批次>/             # 待识别照片
    labels/                   # 授权的真值和质量标签
```

不要把内部人员照片、特征向量、导出的报告或本地数据库提交到公共仓库。导入前应记录授权凭证、用途、采集时间和保留期限。
